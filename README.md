# aw-conductor

## Conductor agentic workflow

Create an issue titled `[Conductor] <summary>` to trigger the Conductor GitHub Agentic Workflow. The workflow applies the Gemini CLI Conductor protocol to scaffold or update `conductor/` context files, generate track specs and plans, and keep the plan status in sync while implementing tasks.
